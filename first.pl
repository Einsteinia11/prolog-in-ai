%Write a program to calculate the sum of two numbers
sum(X, Y, Result):-
    Result is X+Y.

% To run the above code compile buffer then type the function name on
% the console like shown below
% sum(3,4, Result).
